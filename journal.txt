# Sketch Robot 프로젝트 백업/이력

## 환경
- Linux: Ubuntu 24.04, ROS2 Jazzy, Isaac Sim 5.1, RTX 5060
- Windows: Unity 6.4 (sketchrobotunity)
- 통신: ROS-TCP-Connector (147.46.95.52:10000)

## 백업 목록

### Phase 1 stable (2026-04-20)
경로: ~/sketch_robot_ws_BACKUP_phase1-stable_20260420
내용:
- Isaac Sim UR10 + 벽 + 토치 EoAT
- moveit_executor (2-stage: IK approach + cartesian)
- weld_visualizer (TF 기반 비드)
- Phase 1 sketch_ui (Python tkinter)

### Phase 2 complete (2026-04-27)
경로: ~/sketch_robot_ws_BACKUP_phase2-complete_20260427
Unity 백업: C:\Users\min67\sketchrobotunity_BACKUP_phase2-complete_20260427
내용:
- Phase 1 의 모든 것
- Unity SketchManager.cs (클릭→3D, ROS 발행)
- launch 모듈화: core / phase1_python / phase2_unity
- EE orientation 하드코딩: (0.726, 0.668, -0.111, 0.122)

알려진 한계:
- approach 단계 충돌 회피 약함 (Phase 3 에서 개선)
- ros_tcp_endpoint 메모리 누수 (주기적 재시작 필요)

## 핵심 상수
READY_POSE = [-0.08, -1.6, 1.76, -1.76, -1.9, 3.14]
FIXED_TOOL0_QUAT = (0.726, 0.668, -0.111, 0.122)
TORCH_LENGTH = 0.25 m
WALL: position (1.05, 0, 0.2), size (0.1, 1.5, 0.4)
CAMERA_EYE = (-1.1, -1.5, 0.8)
CAMERA_TARGET = (1.0, 0, 0.2)
CAM_W, CAM_H = 320, 240

## 복원 절차
1. 백업 폴더 → ~/sketch_robot_ws 로 복사
2. cd ~/sketch_robot_ws
3. colcon build --packages-select sketch_control --symlink-install
4. source install/setup.bash
5. 사용:
   - Phase 1 (Python GUI): ros2 launch sketch_control phase1_python.launch.py
   - Phase 2 (Unity GUI):  ros2 launch sketch_control phase2_unity.launch.py

## prompts/ 폴더 (2026-04-27 추가)
경로: ~/sketch_robot_ws/prompts/
용도: claude.ai 에서 설계한 긴 prompt 들의 .md 보관소
컨벤션: YYYY-MM-DD_phaseN_<task>.md
README: prompts/README.md 참조

## Phase 3 진행 중 백업 (2026-04-27)
경로: ~/sketch_robot_ws_BACKUP_phase3-stuck_20260427

### Phase 3 작업 요약
- moveit_executor.py 4-stage 파이프라인 (OMPL/cartesian) 구조 완성
- 안전성 보강 (scene_confirmed wait, planner_id retry, executing flag fix)
- OMPL planner 등록 (시스템 ompl_planning.yaml 수정, sudo)
- attached torch collision shape axis 수정 (+Z → +Y)
- ApplyPlanningScene service 호출 추가 (publish 만으로는 불충분)

### 작동하는 시나리오 (단일 점)
- PLANNER_ID = "PTP" (Pilz) 로 단일 점 작동 OK
- 단, 두 번째 Submit (retreat 위치 → 새 안전점) 에서 wall 살짝 닿음

### 미해결 핵심 이슈
- /apply_planning_scene service 호출 시 success=True 응답
- 하지만 /get_planning_scene 으로 확인하면 world.collision_objects=[] (wall 미등록)
- frame_id 'base_link', 'world', 'World' 모두 시도했지만 동일 증상
- TF 에서 'world' 가 invalid frame ID 라는 경고 (그런데 World→world static transform 은 있음)
- ROS2 MoveIt (Jazzy) 의 frame/scene 설정 깊은 이슈 추정

### 다음 세션 시작점
- 백업: sketch_robot_ws_BACKUP_phase3-stuck_20260427
- prompts/ 폴더에 모든 패치 .md 보관됨
- 진단 명령들 (service call 등) 대화 이력에 있음
- 다음 접근 후보:
  1. MoveIt frame 이슈 깊이 진단 (URDF root, planning_frame param)
  2. Workaround: Stage 1 도 cartesian path (OMPL 안 씀)
  3. Workaround: READY_POSE 거치는 다단계 PTP

### 현재 PLANNER_ID, SAFETY_OFFSET 상태
- 패치 후 마지막 적용 값 확인 필요 (코드에 PTP 인지 RRTConnect 인지)

### Phase 3 — 결론

✓ 4-stage pipeline (OMPL Stage 1 + cartesian Stage 2/3/4) 작동
✓ Stage 5 (READY_POSE 복귀) 추가 작동
✓ OMPL planner config 활성화 (시스템 yaml: bak_phase3 백업 존재)
✓ 1차 Submit: 완전 정상

✗ 2차 이상 Submit 의 Stage 1 에서 토치가 wall 살짝 통과
✗ 원인: PlanningScene 에 wall 미등록
   - ApplyPlanningScene service 호출 → success=True 응답
   - 그러나 GetPlanningScene 결과 world.collision_objects=[]
   - frame_id 시도: base_link, world, World — 모두 동일 증상
   - TF tree 진단: world/World 와 base_link 간 TF 끊김 (Isaac Sim 의 World ↔ UR chain root 단절)
   - ROS2 Jazzy + UR + Isaac Sim + ur_moveit_config 의 누적 셋업 이슈로 추정

→ 시뮬에서 wall 등록 디버깅 보류. 실 로봇 셋업 시 재검토.

## 2026-04-27 — 이번 세션 종료, Phase 4 plan 결정

### 결정
- 시뮬 (Isaac Sim) 의 wall 등록 디버깅 보류
- 실로봇 다이렉트 진행
- 단, 로봇이 UR10 → RB10-1300 (Rainbow Robotics) 으로 바뀜
  - URDF, MoveIt 셋업, READY_POSE, EE quat 모두 다시 해야 함
- Depth 카메라: ZED 2 / 2i 사용

### 알려진 사실
- Rainbow Robotics RB10-1300, ROS2 driver 사용 중
- 시뮬 코드의 moveit_executor 4-stage 구조 자체는 재사용 가능
- targets.py / weld_visualizer / Unity 측 코드는 변경 작음
- isaac_sim_ur10.py 는 사실상 무용 (실로봇이라 시뮬 안 씀)

### 다음 세션 사전 정보 수집 필요
- RB10 driver 패키지 이름, URDF 위치
- RB10 spec (reach, payload, joint limits)
- ZED 모델 + 마운팅 계획 (eye-in-hand vs eye-to-hand)
- 작업 환경 (작업물 종류, 안전 셋업)
- RB10 통신 (IP, 네트워크)

### 다음 세션 plan 후보
1. RB10 driver + 기존 코드 포팅 먼저
2. ZED 인식 알고리즘 먼저 (mock 좌표계)
3. 둘 동시 (병렬 트랙)
## 2026-04-29 — Phase 4 Session 1: RB10 driver up on fake_hardware

새 컴(snucem, U24.04+Jazzy)에서 시작. 옛 ws 코드 안 들고 옴 (의도적).

### 한 일
- rbpodo C++ lib v0.16.10 설치 (/usr/local)
- rbpodo_ros2 5 packages clone+build (Jazzy 의존성으로 패키지명 humble→jazzy)
- 빌드 1 warn (on_init deprecated, 무해)
- moveit.launch.py + use_fake_hardware:=true 로 RViz/MoveIt 정상 표시 확인
- 로봇은 끈 채로 정보 추출만

### RB10 정보 (UR10에서 변경되는 핵심)
- model_id: rb10_1300e_u (E-version, U mounting)
- PLANNING_GROUP: "mainpulation"  ← SRDF 오타. 그대로 써야 함
- BASE_FRAME: "link0" (was base_link). world→link0 fixed_joint
- EE_LINK: "tcp" (was tool0)
- robot URDF name: "rb"
- JOINT_NAMES (운동학순): base, shoulder, elbow, wrist1, wrist2, wrist3
- /joint_states 발행순(알파벳): base, elbow, shoulder, wrist1, wrist2, wrist3 ← swap 주의
- Controller: joint_trajectory_controller (FollowJointTrajectory) + joint_state_broadcaster
- Update rate: 30Hz
- SRDF에 group_state(named pose) 없음 → READY_POSE 직접 정의 필요
- joint limits 다 ±3.14/3.14/10 placeholder ← 실로봇 단계에서 데이터시트 확인 필요

### 다음 세션
A: 위 비교표로 깨끗한 포팅 prompt → Claude Code에 던져 moveit_executor.py 변환
   (READY_POSE_JOINTS, FIXED_TOOL0_QUAT는 TODO 마킹)
B: 실로봇 첫 연결 (E-stop·EoAT 분리·1.3m 클리어 후 use_fake_hardware:=false)
C: teach pendant로 안전 자세 → /joint_states 읽어서 READY_POSE 실측
D: (보류) joint limits 실값
E: (병렬 트랙) ZED 드라이버만 띄워보기

### 산출 파일
- output/phase4_session1_rb10_driver_setup.md  (전체 정리)

============================================================
2026-05-05 Phase 4 Session 4 — RB10 motion pipeline 검증
============================================================

목표: Stage 1 단독 검증 + 안전한 retry 메커니즘 정착

진행:

1. Stage 1 단독 trigger 추가
   - patch: prompts/2026-05-05_phase4_stage1_trigger.md
   - /debug_trigger_stage1 (Bool), Stage 5 와 동일 패턴
   - DEBUG_STAGE1_OFFSET=0.15m (production offset 과 분리)
   - publish_test_waypoint.py 신설 — tf2 로 link0->tcp 실측 orientation 사용
     (RB10 wall-facing quat 미정인 상태 우회)
   - stage1_approach_free 에 on_complete 인자 추가 → Stage 2 chain 차단 가능
   - 회귀 가드: _stage1_result 의 'or stage2_approach_linear' fallback 으로 production 흐름 보존
   - colcon build OK

2. Stage 1 단독 1차 시도 — 사고 발생
   - waypoint (0.80, 0, 0.45), safe_pose (0.65, 0, 0.45)
   - planning OK 10 포인트, 16ms (서버 reject 없이 즉시 success)
   - FollowJointTrajectory 전송 약 1.05초 후 controller deactivate
     log: "Trajectory 실행 에러 code=-1: Current goal cancelled during deactivate transition"
   - 로봇이 swooping path 로 +Y 방향 휘두르며 캐비닛 (사진 왼쪽 백색 박스) 윗면 임팩트
   - RB10 내장 충돌 감지 → 자체 안전정지 발동
   - 본인 / 로봇 / 캐비닛 모두 큰 손상 없음

   원인:
   - planning scene 에 'wall' + 'brush' 만 등록, 캐비닛/책상/주변 장비 미등록 → OMPL 자유공간 인식
   - Cartesian goal + READY 의 wrist-up orientation 강제 → IK 해 공간 좁아 OMPL 우회 path 선택
   - rescale 0.3x 도 인간 반응 시간 대비 빠름 (10 포인트 / 사실상 1~3초 모션)

3. Joint-space jog trigger 추가
   - patch: prompts/2026-05-05_phase4_jog_debug.md
   - /debug_trigger_jog (Bool) — OMPL/IK/planning scene 모두 우회
   - JointTrajectory 직접 빌드 → execute_trajectory_direct
   - wrist3 (idx 5) 만 0.05 rad, 10s, 50점 선형 보간
   - Cartesian Stage 1 trigger 와 분리된 새 콜백 (회귀 없음)
   - msg.data 가드 추가 (Bool false 무시)

4. Joint-space jog 검증 통과
   - trigger 수신 + trajectory 전송 정상
   - TCP_NOW quat 변화로 회전 확인:
     before: quat=(-0.000, 0.707, 0.707, 0.000)
     after:  quat=(-0.018, 0.707, 0.707, -0.018)
     local axis 들 0.05 rad 만큼 회전, commanded 와 정확히 일치
   - controller deactivate 없이 10초 정상 완료
   - 시각으로는 못 봤지만 (토치/마커 없는 flange 자전) 데이터상 명확

검증 완료 항목:
- ROS -> moveit_executor -> controller -> 실로봇 신호 경로 정상
- joint_command publish 정상
- execute_trajectory_direct 동작
- trajectory 완료 콜백 (Stage chain 차단) 정상
- RB10 자체 충돌감지 안전망 정상 작동

미해결 이슈 (다음 세션 처리):
- planning scene 의 환경 collision objects (캐비닛/책상/주변 장비) 미등록 -> ZED 로 해결 예정
- 펜던트 속도 override 가 ROS trajectory 에 적용되는지 미검증 (시간 부족)
- 펜던트 wrist3 표시 (97.38°) vs joint_states wrist3 값 (69.4°) 불일치
  -> joint name 매핑 또는 zero offset 차이 추정, ZED 셋업 시 TF 검증과 같이 확인
- READY_POSE_JOINTS 코드값이 사고 전 자세 (base ≈ +179°) 그대로
  -> 현재 펜던트 자세 (base ≈ 3.45° 등) 와 다름. Session 5 시작 시 갱신 필요
  -> 갱신 전엔 /debug_trigger_stage5 사용 금지 (큰 자세 차이로 위험)

다음 세션 (Session 5) 계획:
- READY_POSE_JOINTS 갱신 (현재 펜던트 자세 또는 새로 정한 안전 자세)
- ZED 통합:
  * 마운팅 위치 결정 + 측정
  * SDK + ROS2 wrapper 설치 (CUDA / RTX 5060 호환)
  * base_link -> ZED frame static TF
  * PointCloud2 받기 검증
- 점군 -> octomap (또는 collision object) 변환 회로
- 보드 표면 분리 (RANSAC plane) 로 target 영역은 collision 에서 제외
- 그 후 Stage 1 Cartesian goal 재시도 (planning scene 완성 상태)

백업: 이번 세션은 코드 변경만. 큰 마일스톤 백업은 ZED 통합 완료 후로.

